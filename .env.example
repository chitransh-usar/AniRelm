# Server Configuration
PORT=3000
NODE_ENV=production

# Database Configuration - IMPORTANT FOR RENDER
# For MongoDB Atlas (Recommended for Render):
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/pinterest-clone?retryWrites=true&w=majority

# For local development:
# MONGODB_URI=mongodb://127.0.0.1:27017/clone

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-here

# Render-specific settings
# These will be automatically set by Render
# PORT=10000 (Render sets this automatically)
# NODE_ENV=production (Render sets this automatically)
